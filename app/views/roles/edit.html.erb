<h1>Editing Role</h1>
<% if current_person.is_admin? %>
<%= render 'form' %>

<%= link_to 'Show', @role %> |
<%= link_to 'Back', roles_path %>
<% else %>  
  Sorry, must be Admin <%= link_to 'Back', root_path %> 
<% end %>
